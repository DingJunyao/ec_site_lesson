<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %> - 淘书网</title>
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="stylesheet" href="/stylesheets/detail.css">
  </head>
  <body>
    <div class="container">
      <%- include("./header.ejs") %>
      <div class="func-area">
        <% if(book != '') { %>
          <% if(book.fldNumber > 0) {var storage = '有货';} else {var storage = '无货';} %>
          <div class="book-img">
            <img src="/images/<%= book.fldPicture %>" alt="<%= book.fldBName %>">
          </div>
          <div class="book-desp">
            <h1 class="book-name"><%= book.fldBName %></h1>
            <p class="author"><span class="content-title">作者</span>：[<%= book.fldAuthorCountry %>] <a href="/list/author/<%= book.fldAuthor %>"><%= book.fldAuthor %></a></p>
            <p class="press"><span class="content-title">出版社</span>：<a href="/list/press/<%= book.fldPress %>"><%= book.fldPress %></a> <span class="content-title">出版时间</span>：<%= book.fldDate %></p>
            <p class="price"><span class="content-title">淘书网价</span>：<span class="price-num">￥<%= book.fldPrice %></span></p>
            <p class="desp-title"><span class="content-title">内容简介</span>：</p>
            <p class="desp-content"><%= book.fldDescription %></p>
            <p class="to"><span class="content-title">配送至</span>：<input type="text"> <span class="storage"><%= storage %></span> 免运费</p>
            <p class="service"><span class="content-title">服务</span>：由“淘书网”发货，并提供售后服务。</p>
            <div class="operation-area">
              <input type="number" class="buy-quantity" value="1" max="<%= book.fldNumber %>" min="1">
              <button class="add-to-cart">加入购物车</button>
              <button class="buy">立即购买</button>
              <button class="buy-ebook">购买电子书</button>
            </div>
          </div>
        <% } else { %>
          <h1>抱歉，没有相关书籍</h1>
        <% } %>
      </div>
      <%- include("./footer.ejs") %>
    </div>
  </body>
  <script src="/js/global.js"></script>
  <script src="/js/search.js"></script>
</html>
